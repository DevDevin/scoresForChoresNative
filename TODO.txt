-- remove the auto filled values from the form like email and password
-- make sure form is cleared out when opening form to add new reward, chore, user, etc...
-- fix color scheme
	-change colors of lists if necessary
	-fix color of side menus
-- make the login and startup pages more responsive
-- change all hardcoded values to percentages
-- test out on a tablet simulator
-- fix side manager layout, color, size etc... 
-- test reset chores, style if necessary
-- test reset passwords
	- fix style to look like every other form. 
-- test forgotten password and style if necessary
-- remove any unsecure keys to the database. replace with tokens or whatever is needed (vulnerabilities described in git)
-- add functionality so that deleting users also deletes corresponding reward requests, earned rewards, chores
-- add a cancel button in the create chore form. 
-- re style the edit user modal 
-- add/fix functionality so that when a completion request is accepted. The earned points of that child is updated.
-- when a completion request is accepted it loses it's isRecurring attribute. It needs to keep it
	- this is important for resetting chores
	- make sure that isn't happening when it is rejected 
